
  # Generate interface code

  This is a code bundle for Generate interface code. The original project is available at https://www.figma.com/design/1EfaWmOOfGlnIpOcARHmbK/Generate-interface-code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  